function displayUpdate() {
    let initialValue = "0";
    const display = document.querySelector('.calculatorScreen');
    display.textContent = initialValue;
}

window.onload = displayUpdate;

function operate(operator, num1, num2) {
    
}